import styled from "styled-components";
import { Form as FormikForm } from "formik";
import { layout, space } from "styled-system";

const Form = styled(FormikForm)(layout, space);

export default Form;
